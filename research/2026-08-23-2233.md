# Pass 1 — A1 — 2026-08-23
**Task:** A1 [open] Suite + environment baseline — run pytest + ruff from .venv, record exact counts, fix rot with minimal diffs.
**Since last pass:** none (first pass; no loop commits before this one).

## What landed
- `f57a913` loop: A1 lint baseline — fixed all 19 ruff violations; suite green.

## Measurements
- `pytest tests/ -q` (before fixes): **510 passed**, 48 warnings, 99.67s. No failures — zero test rot.
  - Note: CLAUDE.md claims "424 tests" and "~9s"; both stale. Real counts: 510 tests, ~95–100s wall on RTX 2060 SUPER.
- `ruff check src/ tests/` (before fixes): **19 errors** (CLAUDE.md claims "0 errors" — stale). Breakdown:
  E402 ×1 (cli/app.py), SIM105 ×3 (backtest.py, regime.py, stages.py), SIM222 (discover.py `output or True`),
  UP042 ×4 (types.py enums), SIM108 ×5 (sources.py, wavelet_features.py, patterns.py, wavelets/features.py ×2),
  SIM101 (storage.py), B905/B007/SIM113 (wavelet_lstm.py), A002 (test_library.py).
- `ruff check src/ tests/` (after fixes): **All checks passed!**
- `pytest tests/ -q` (after fixes): **510 passed**, 93.43s.
- Environment: ruff 0.15.1, torch verified via suite run (GPU AMP warnings present as expected).

## Findings / surprises
- The lint rot was real code drift, not a config change on our side: recent commits (dashboard/exp3 work)
  landed files that violate the repo's own strict `[tool.ruff.lint]` select set.
- Two violations were NOT mechanically fixable and were handled in config instead of code:
  - `UP042` (str+Enum → StrEnum) ×4: converting changes `str()` semantics of MarketLabel/RegimeType/
    AssetClass/Sector across the codebase — too behavior-risky for a lint cleanup. Added to global ignore
    with comment in `pyproject.toml`.
  - `E402` in `src/wavecast/cli/app.py`: `load_dotenv()` deliberately runs before command imports. Added
    per-file-ignore (same pattern the repo already uses for dashboard/app.py).
- All other 13 fixes were mechanical and behavior-preserving (contextlib.suppress, ternaries, merged
  isinstance, zip strict=True, enumerate, builtin-shadow rename in test helper).
- `.gitignore` excludes `research/` — report needed `git add -f`. Future passes should remember this.

## Blockers / for Aaron
- None. Optional: CLAUDE.md's Testing section (424 tests / ~9s) could be updated to 510 / ~95s — left
  untouched to keep this diff minimal.

## Next
A2 [open] Massive fetch resilience — 429-aware retry w/ backoff + Retry-After, inter-ticker pacing,
per-ticker error isolation in ForwardTestRunner; unit tests with mocked 429s.
