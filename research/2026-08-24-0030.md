# Pass 10 — B3 timescale sweep — 2026-08-24
**Task:** B3 [open] Timescale sweep — daily vs hourly, compared at comparable
turnover (orchestrator note: reuse B1 grid + B4 holding overlay; per-bar hourly
is a cost floor). Flag hold=5 walk-forward stability as a B5 candidate.
**Since last pass:** B4 landed (72758f8 module, 9b4fe0e runner, 0c704c7 report).

## What landed
- `0d2ffb2` `signals/timescale.py` (annual-turnover normalization: `bars_per_year`,
  `annual_turnover`, `match_by_turnover`) + `scripts/timescale_sweep.py` + 4 tests.
- B3 sweep run: 400 rows appended to `research/results.jsonl` (task="B3").
- Ledger/INDEX updates; B3 done.

## Measurements
Command: `PYTHONPATH=. .venv/bin/python scripts/timescale_sweep.py`
(20 tickers × {1h,1d} × {persistence, mean_reversion} × hold {1,2,3,5,10},
flat sizing, train_end 2024-06-30, 7bps round-trip; test-window mean Sharpe,
mean annualized position-changes/year):

```
ivl  rule             hold test_mean  pos  ann_turn n
1d   mean_reversion      1    -0.245    7     126.4 20
1d   mean_reversion      2    +0.149   12      62.3 20
1d   mean_reversion      3    -0.021   10      44.3 20
1d   mean_reversion      5    +0.303   11      24.9 20
1d   mean_reversion     10    +0.288   12      13.2 20
1d   persistence         1..10 all negative (-0.206 best)
1h   mean_reversion      1    -1.001    0     953.8 20
1h   mean_reversion      5    -0.280    0     184.8 20
1h   mean_reversion     10    -0.190    2      90.7 20
1h   persistence         1    -1.584    0     953.8 20
1h   persistence        10    -0.056    9      90.7 20
```

**Verdict:** daily dominates hourly at EVERY comparable turnover band. At the
only near-matched pair (daily h2 ≈62 chg/yr vs hourly h10 ≈91 chg/yr), daily
mean-reversion is +0.15 vs hourly's best −0.06. Hourly never crosses zero for
mean-reversion and only approaches it via extreme holding (h10), which is just
a slow daily strategy with worse sampling. The B1 "hourly is cost-dead" finding
survives turnover matching — it's not only costs; hourly signal quality itself
is worse.

B4's headline reproduces in this run: daily mean_rev h5 +0.303 (11/20 tickers
positive) — consistent with pass 9's +0.30.

## Findings / surprises
- Turnover-matching does NOT rescue hourly: even at ~90 changes/year hourly is
  ≤0 while daily at ~60/year is positive. Timescale conclusion: **daily bars are
  the operating timescale**; hourly needs no further sweep work.
- Persistence stays negative at every interval/hold — confirms A4/B4.

## Blockers / for Aaron
- **Remaining half of B3 (model rule on daily):** original task text includes
  retraining WaveletGPT on daily bars so the model rule can be compared across
  intervals. Rules-based half is done above. Small local trainings are allowed
  but this also needs a daily SAX/vocab pipeline decision — propose as B5 work
  rather than improvising it here.
- **B5 candidate (per orchestrator):** walk-forward stability check of the hold
  parameter — B4/B3's h5 result is picked from {1,2,3,5,10} on the same test
  window used to report it; before treating hold=5 as a setting, refit hold per
  expanding window and measure how often h5 is re-selected OOS.

## Next
- B5: hold-parameter walk-forward stability (recommended first), then the
  daily-model-retrain comparison if Aaron nods.
