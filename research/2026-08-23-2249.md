# Pass 2 — A2 Massive fetch resilience — 2026-08-23
**Task:** A2 [open] — 429-aware retry w/ backoff + Retry-After, inter-ticker pacing, per-ticker error isolation in the forward runner.
**Since last pass:** f57a913 (A1 lint baseline), b93ecca (A1 report+INDEX).

## What landed
(skeleton — fill before commit)

## Measurements
(pending)

## Findings / surprises
- `fetch_massive` and `fetch_massive_ohlcv` duplicate the identical `client.list_aggs` loop with a bare `except Exception -> DataError` (sources.py:96, :222). Any 429 surfaces once and kills the call — retry must wrap this shared path.
- Forward runner `run_once` (forward/runner.py:116) has NO try/except around `fetch_latest_bars` per ticker — one DataError aborts the whole cycle. Confirms the cron.log JPM failure mode.

## Blockers / for Aaron
(none yet)

## Next
(census for A3)
