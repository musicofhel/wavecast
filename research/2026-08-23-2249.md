# Pass 2 — A2 Massive fetch resilience — 2026-08-23
**Task:** A2 [open] — 429-aware retry w/ backoff + Retry-After, inter-ticker pacing, per-ticker error isolation in the forward runner.
**Since last pass:** f57a913 (A1 lint baseline, 510 tests green), b93ecca (A1 report+INDEX).

## What landed
- **94687d5** `loop: A2 429-aware retry + backoff in Massive fetch, per-ticker isolation + pacing in forward runner`
  - `src/wavecast/data/sources.py`: new `_fetch_aggs_with_retry()` — up to 5 attempts on 429, exponential backoff (base 2s, cap 60s), honors `Retry-After` header; non-429 errors fail fast. `_is_rate_limit_error()` detects via `status_code` attr or message ("429"/"rate limit"/"too many requests"). Both `fetch_massive` and `fetch_massive_ohlcv` now share this helper (previously duplicated loops).
  - `src/wavecast/forward/runner.py`: `fetch_latest_bars` per (ticker, interval) wrapped in try/except `(DataError, DataNotFoundError)` → warn + skip; new `ForwardTestRunner(config, pacing_seconds=1.0)` sleeps between tickers (0 disables).
  - Tests: `tests/unit/test_sources_retry.py` (8) + 4 appended to `tests/unit/test_forward_runner.py`.

## Measurements
- Narrow: `pytest tests/unit/test_sources_retry.py tests/unit/test_forward_runner.py -q` → **19 passed**.
- Full: `pytest tests/ -q` → **522 passed** in 95s (was 510; +12 new). `ruff check src/ tests/` → clean.

## Findings / surprises
- The two Massive fetchers duplicated an identical bare `except Exception -> DataError` list_aggs loop — any 429 surfaced once and killed the call. Now factored into one retrying helper.
- `ForwardTestSummary.per_ticker` is only populated from resolutions, not predictions — the isolation test asserts against the tracker's `predictions.jsonl` instead.
- Retry detection is string/status-code based because we don't pin the massive client's exception taxonomy (it raises varied types); if a typed exception exists upstream this can be tightened later.

## Blockers / for Aaron
- None.

## Next
A3 — forward-run gap census from `~/.wavecast/forward_tests/d1_forward_v1/cron.log` + ledger timestamps; catch-up runner demonstrated into `loop_scratch/`.
